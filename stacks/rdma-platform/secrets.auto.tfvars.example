# Copy to secrets.auto.tfvars in this directory (the real file is gitignored):
#   copy secrets.auto.tfvars.example secrets.auto.tfvars
# Then set values. Terraform loads *.auto.tfvars automatically from the working directory.

rhsm_org_id         = "your-rhsm-org-id"
rhsm_activation_key = "your-activation-key"
